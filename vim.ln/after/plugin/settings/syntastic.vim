" Always hide the location list. Run :Errors to show errors if wanted
" (they will always be shown in the gutter)
let g:syntastic_auto_loc_list=0

" Set ruby checkers
" let g:syntastic_ruby_checkers = ['mri', 'rubocop']

" Show all errors
let g:syntastic_aggregate_errors=1

" Indicate signs in the gutter
let g:syntastic_enable_signs=1
