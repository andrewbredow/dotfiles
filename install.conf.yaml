- defaults:
    link:
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.agignore: configs/agignore
    ~/.bin: configs/bin
    ~/.config/nvim: configs/vim
    ~/.gemrc: configs/gemrc
    ~/.gitconfig: configs/gitconfig
    ~/.gitignore_global: configs/gitignore_global
    ~/.irbrc: configs/irbrc
    ~/.pryrc: configs/pryrc
    ~/.psqlrc: configs/psqlrc
    ~/.rspec: configs/rspec
    ~/.tmux.colors: configs/tmux.colors
    ~/.tmux.conf: configs/tmux.conf
    ~/.tool-versions: configs/tool-versions
    ~/.vim: configs/vim
    ~/.vimrc: configs/vimrc
    ~/.zplug: configs/zplug
    ~/.zsh: configs/zsh
    ~/.zshrc: configs/zshrc

- shell:
  - [touch ~/.hushlogin, Hush the last login prompt]
  # This might need nome work on non-nvim systems
  - [nvim -u ~/.vim/plugs.vim +PlugInstall +qall, Install Vim plugs]

  # TODO
  # - Install homebrew, casks, packages, fonts, etc...
  # - Install asdf and versioned things
  # - Update OS X and other application settings
