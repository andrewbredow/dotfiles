- defaults:
    link:
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.tmux.conf: configs/tmux.conf
    ~/.tmux.colors: configs/tmux.colors
    ~/.vim: configs/vim
    ~/.vimrc: configs/vimrc
    ~/.config/nvim: configs/vim
    ~/.tool-versions: configs/tool-versions
    ~/.gemrc: configs/gemrc
    ~/.irbrc: configs/irbrc
    ~/.pryrc: configs/pryrc
    ~/.rspec: configs/rspec
    ~/.psqlrc: configs/psqlrc
    ~/.gitconfig: configs/gitconfig
    ~/.gitignore_global: configs/gitignore_global
    ~/.agignore: configs/agignore.ln
    ~/.zplug: configs/zplug
    ~/.zshrc: configs/zshrc
    ~/.zsh: configs/zsh

- shell:
  - [touch ~/.hushlogin, Hush the last login prompt]
  # This might need nome work on non-nvim systems
  - [nvim -u ~/.vim/plugs.vim +PlugInstall +qall, Install Vim plugs]
